# ============================================================
# Supabase Storage to S3 Migration Configuration
# ============================================================
# Copy this file to .env and fill in your values:
#   cp env.example .env
#
# Ensure the Supabase CLI is linked to your project:
#   supabase link --project-ref <your-project-ref>
# ============================================================

# ============================================================
# Supabase Configuration
# ============================================================

# Project reference (from Supabase dashboard URL or `supabase projects list`)
SUPABASE_PROJECT_REF=your-project-ref

# Buckets to migrate (space-separated list, or "all" for all buckets)
# Examples:
#   SUPABASE_BUCKETS="avatars documents"
#   SUPABASE_BUCKETS="all"
SUPABASE_BUCKETS="all"

# ============================================================
# AWS Configuration
# ============================================================

# Target S3 bucket name
AWS_S3_BUCKET=your-s3-bucket

# AWS region for the S3 bucket
AWS_REGION=us-east-1

# S3 prefix (optional) - prepend path to all migrated objects
# Example: S3_PREFIX="supabase-backup/" results in s3://bucket/supabase-backup/bucketname/...
S3_PREFIX=""

# AWS credentials (optional - can use ~/.aws/credentials or IAM role instead)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# ============================================================
# Migration Options
# ============================================================

# Temporary directory for downloaded files
TEMP_DIR=/tmp/supabase-migration

# Clean up temp directory after successful migration (true/false)
CLEANUP_TEMP=true

# Number of parallel jobs for supabase storage cp (default: 5)
PARALLEL_JOBS=5
